<!--components/popover/index.wxml-->
<view class="popover-mask" wx:if='{{visible}}' bindtap="onHide" catchtouchmove="stopMove">
  <view wx:if='{{visible}}' class='popover-view {{vertical}} {{align}}' style='width:{{pw}}px;height:{{ph}}px;left:{{px}}px;top:{{py}}px;' catchtap="stopMove">
    <slot />
  </view>
</view>